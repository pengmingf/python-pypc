1.python调用多线程执行任务的时候，必须要注意加锁，否则容易造成多线程重复执行某一任务。
2.多线程加锁的目标应该单独列出。多线程执行加锁的对象，对于所有线程来说应该是统一对象，否则各个线程拿到的对象不同，加锁也就失去了意义
3.利用python爬取一个网站，最重要的是分析数据。
4.正则表达式是爬虫的基础，许多网站返回值都不会是单纯的json数据。返回一个html页面这种，就需要我们用正则去匹配
5.爬虫应该遵守相关的规范，网站根目录下的roobot.txt文件里面会有可允许爬取的内容。
6.反爬虫的措施？以及如何爬取58同城的数据？有待研究。。。。